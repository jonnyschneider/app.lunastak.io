# Why `prod` has approved drift

in sync

Approved 2026-09-09. Clears when the migration is applied to prod — re-run `npm run db:approve-drift -- --env prod --reason "in sync"` or, once every env is in sync, plain `npm run db:approve-drift`.
